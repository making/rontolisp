# Knowledge Base

Detailed, implementation-level notes on invariants referenced from `CLAUDE.md`.
Each file expands one bullet from CLAUDE.md's "Key Design Constraints" (or a
workflow) with full detail: internal class names, per-backend mechanics, edge
cases, and the tests that pin the behavior. Read the CLAUDE.md summary first;
come here only when you need the "why exactly" behind a constraint.

- [core-representation.md](core-representation.md) -- core value model: symbolp/stringp & consp encodings, three-pass compilation, the `FreeVarAnalyzer` rule a closure's captures depend on (a subform it skips is a silently wrong JVM answer and a WASM compile error), `%` prefix, `BuiltinFunctionWrappers`, JVM method mangling + the JVMCI `%` gotcha, WASM rec-group rule (the "no `.kb` file" core-representation bullets, relocated)
- [hot-path-method-size.md](hot-path-method-size.md) -- HotSpot's HugeMethodLimit (8000 bytecodes, silently uncompiled past it): the `evalCons` split and the `_invoke_<arity>` segment budget + binary-search dispatch, with the measurement that pins both
- [integer-bitwise-fast-paths.md](integer-bitwise-fast-paths.md) -- `logand`/`logior`/`logxor`/`lognot`/`ash`/`integer-length`/`logbitp` fixnum fast paths (interpreter + JVM; WASM already had one) and the literal-byte-specifier fold in `ldb`/`dpb`/`mask-field`
- [argument-evaluation-order.md](argument-evaluation-order.md) -- the argument forms of a call, and the elements of `list` (backquote and `make-array :initial-contents` with it), evaluate LEFT TO RIGHT on every backend: why only the cons-chain-linking emitters broke, the three field sightings (the last one local-time's TZif header reading its six fields in reverse), and why `ArgumentOrder` refuses to treat a bare variable reference as reorderable
- [lisp2-namespaces.md](lisp2-namespaces.md) -- Lisp-2 function/variable namespace split across all backends
- [lambda-lists.md](lambda-lists.md) -- lambda list extensions (`&optional`/`&rest`/`&key`/`&aux`) desugared to "required + `&rest`"
- [do-return-block.md](do-return-block.md) -- `do`/`return` and the `%block` non-local exit boundary; `block`/`return-from`, LEXICAL on every backend (the interpreter's block scope IS the block's identity); `catch`/`throw` (dynamic tag-keyed exits over the same machinery); `tagbody`/`go` + `prog`/`prog*`, including the cross-lambda `go` re-entry loop
- [loop-iteration-heads.md](loop-iteration-heads.md) -- `loop`'s per-clause iteration heads: which assignments happen BEFORE the termination test (element variables, so a closure/`finally`/a later clause sees the last element) vs AFTER it (`on`'s cursor, `being the hash-key`, which end at nil in CL too), `repeat` as a clause-ordered driver, the deliberate eager-binding divergence from SBCL, and why the numeric head's exact expansion shape is load-bearing for the wasm counted loop
- [jzon-cl-additions.md](jzon-cl-additions.md) -- the jzon-driven all-backend CL additions (shiftf/typep/subtypep/`|...|`/`#'format`/mutable strings/runtime type-dispatch sweep &c), pinned by `runtime-type-dispatch-residue` + `JzonE2eTest`
- [defmacro-backquote.md](defmacro-backquote.md) -- `defmacro`, read-time backquote, compile-time macro expansion
- [format.md](format.md) -- `format`'s TWO renderings of one directive set: the compile-time expansion of a literal control vs. the Lisp-source runtime renderer (`format-render.lisp`, `%fmt-render`) every runtime control funnels into, the once-per-program injection gate, why the `macro` package sits above `reader`, what `~F`/`~$` lower to (one `%fixed-decimal` call, and why the inline expansion must not come back), the deliberate divergences (the renderer never signals), and the FLOAT PRINTER: one Schubfach shortest-round-trip selection (`FloatText`) on all four backends, the ~755-byte compressed power table with emit-time-verified corrections, and the single-float array-element width
- [pretty-printer.md](pretty-printer.md) -- the printer entry points (`write`/`pprint`), the real pprint DISPATCH tables, `pprint-logical-block` and the format logical block: what a stream with NO column can and cannot do (every conditional line break is a no-op, nothing wraps, `*print-right-margin*` is inert), the printer-control variable table whose every default IS the printer's behavior, `*print-case*`'s one shared `%print-cased` renderer and its mention-the-variable gate, and the re-evaluation trigger
- [gensym-macroexpand.md](gensym-macroexpand.md) -- `gensym`, `macroexpand`/`macroexpand-1`
- [compiler-macros.md](compiler-macros.md) -- `define-compiler-macro` applied at call sites + `load-time-value` evaluated once per occurrence (the pair that makes a library's own `(constantp arg)` -> `(load-time-value (build-it ...))` optimization run): the two application seams, the three load-bearing properties (decline must terminate, a signalling body must not fail the compile, expansion-time output is muted), the compile-path hoist into `%LOAD-TIME-VALUE-N` slots and why the fill is lazy
- [symbol-runtime-api.md](symbol-runtime-api.md) -- `symbol-name`/`intern`/`find-symbol`/`make-symbol`/`boundp`/`fboundp`/`symbol-value` (verbatim names, no intern table, global-only variable lookups) + the package API (`find-package`/`symbol-package`/`type-of`, a "package" = its upcased name as a keyword), quoted-lone-symbol resolution, and the one-table seeding that keeps a compiled special's two homes (global cell / eval mirror) from disagreeing about the standard stream variables, plus the ACCESSIBILITY STATUS second value of `find-symbol`/`intern` (one shared compile-path fold, nil together with the symbol) and the three lowering bugs `(find-symbol "CAR" 'common-lisp)` had to shed, and the STRING-DESIGNATOR contract -- `(string ...)` is the one coercion every designator position routes through, it type-checks on all four backends, and the file enumerates exactly which positions are designators (the trim BAG and `concatenate` are not)
- [defstruct.md](defstruct.md) -- `defstruct` expansion into plain defuns, instance-object representation, setf accessor registry, `:include` inheritance + `:type (vector ...)`
- [instance-syntax.md](instance-syntax.md) -- the ONE instance value model behind defstruct/CLOS/conditions: the `%obj-*` primitive seam, `#S(...)`/`#<...>` printing and `#S(...)` source reading, per-backend shapes (JVM `String[]` discriminator, WASM `TYPE_INSTANCE` + bake-all), the emit gate (which the three FIXED layouts `%PATHNAME` / `%SYNONYM-STREAM` / `%STREAM` also turn on), and the structural-`equal` decision record
- [clos.md](clos.md) -- static CLOS subset (`defclass`/`defgeneric`/`defmethod`/`make-instance`/`slot-value`): shared registry, dispatcher generation (a methoded BUILT-IN name stays as the default method), cl-who expansion-time dispatch, the instance-initialization protocol + runtime `typep`
- [make-load-form.md](make-load-form.md) -- a LITERAL object in code the compile path compiles is dumped by its own `make-load-form` method (cffi's parsed foreign type, in every `defcfun` body), substituted once in `eval/LoadFormSubstituter` at the end of the macro-time pass; why the built-in default stays the quote compilers' structural dump, what `load-time-value` buys, and the init-form / nested-object / quoted-constant shapes
- [gray-streams.md](gray-streams.md) -- rontolisp's own Gray-stream protocol (gray.lisp class hierarchy + write/read/peek/unread/sequence/file-position generics, the ONE required method per direction, :eof convention, the protocol's one-slot pushback), the interpreter built-in wraps, the compile-path `GrayStreamsLibrary.process` call-site rewrite + on-use dispatch splice, the HANDLE-side `unread-char` pushback (`UnreadCharLibrary` / `unread-char.lisp`), the trivial-gray-streams shim, and flexi-streams' own Gray classes -- the in-memory octet pair AND the `flexi-stream` WRAPPER (a UTF-8 codec over the octet stream it wraps, and why it deliberately has no `close` method), plus why `streamp` / `(typep x 'stream)` answer t for a Gray instance and where the three halves of that live (the `streamp` lowering, the runtime-typep table, `ArgumentShapes`' dead-branch pruning)
- [packages.md](packages.md) -- the `cl`/`cl-user`/`rontolisp` package system, `defpackage` over an EXISTING package MODIFYING it (CLHS, and what makes a seeded shim package declarable by the real library), and `*package*` as a DYNAMIC variable (the package keyword, read at run time on every backend; `in-package` resolves to `(setq *package* :P)`; the interpreter's variable IS the resolver's current package)
- [reader-case-upcase.md](reader-case-upcase.md) -- the reader's CL-style uppercase-canonical model (upcase, no fold), verbatim `intern`/`find-symbol`, and the remaining case-tolerance seams (keyword-arg matching, WIT/host lower-kebab bridges)
- [characters-code-points.md](characters-code-points.md) -- CHARACTER = Unicode code point on every backend: JVM widening to `int[]{cp}`, WASM `_char_upcase`/`_char_downcase` case-fold table, code-point string indexing (`length`/`char`/`subseq`)
- [string-index-cost.md](string-index-cost.md) -- a character index costs the same wherever it lands, so a string scan is LINEAR on all four backends: why an index is not a memory offset (UTF-16 on the JVM, UTF-8 on WASM), the per-string character-index cursor WASM's `TYPE_STRING` carries (the two facts it encodes, the store rule, why an immutable string makes it sound) and the JVM's surrogate-pair proof with its two-slot memory (why the fields are plain, not volatile), the before/after table, and the three shapes that are still not constant
- [reader-features.md](reader-features.md) -- `#+`/`#-` feature conditionals, `*features*` (a run-time VARIABLE on every backend, and why a source's own literal push reaches its own `#+` while a computed one cannot), `#|...|#` block comments, `#.` handling, `#N@(...)` + the readtable no-ops, and the three TARGET-SHAPE features beside the backend one (`rontolisp-reactor`, `rontolisp-component`, `rontolisp-body-imports` -- and why a guard names the thing it branches on rather than the targets that lack it)
- [directory-listing.md](directory-listing.md) -- the ONE directory-listing primitive (`%list-directory`) and the family of Lisp spellings above it (`directory`, the `uiop:` walkers, `pathname-directory`): the `(t . names)` contract and why the leading `t` is load-bearing, the ninth preview1 import, the component adapter's `stream<directory-entry>` / cookie-as-index / short-round traps, and why `directory` is ANSI's matching operator (SBCL-checked) rather than the bare listing the todo's non-goal first suggested
- [pathnames.md](pathnames.md) -- a pathname is a distinct VALUE (the fixed `LispLayout.PATHNAME` instance layout, seeded layout-only like `%UNBOUND%`): `#P"..."` in the frontend AND both emitted readers, the per-kind printer arms (prin1 `#P"ns"` / princ bare, nested included), the producers-wrap / consumers-coerce / internals-stay-strings prelude pattern, the read-forces-the-instance-gate coupling, the folder folding to pathname literals, what retired `pathnameClauseYields`, and the lack/jzon/mito shapes that pin it
- [binary-sequence-io.md](binary-sequence-io.md) -- `read-sequence` / `write-sequence` over a PACKED buffer (a packed float array of any rank, a packed `(unsigned-byte 8|16|32)` vector) move raw little-endian elements in ONE native transfer on every backend: the `%read-sequence-packed` / `%write-sequence-packed` declining primitives in front of the element loop, per-backend mechanics (interpreter `ByteBuffer`, JVM `_readSeqPacked` gated by `usesPackedSequenceIo`, wasm `_read_packed` chunking through `HEAP_PTR` incl. the `--simd` vblock arm), why little-endian
- [read-load-streams.md](read-load-streams.md) -- `read`/`load`/`read-line`/file streams runtime; **a stream is a self-describing VALUE** (the fixed `%STREAM` layout, its handle/kind slots, the one `usesStreamValues` gate over both the producer wrap and the consumer unwrap, and the exact `file-stream`/`string-stream` tests that follow); binary stdin/stdout as the standard-stream designator; component stdin (stdin.lisp); a SYNONYM stream as a distinct value forwarding per operation (the reader closure, the one `%STREAM-TARGET` resolution every seam funnels through, and the "the program spells `make-synonym-stream`" gate)
- [standard-output-redirect.md](standard-output-redirect.md) -- the print/read families resolve their stream DESIGNATOR (omitted argument AND explicit nil) through `*standard-output*` / `*standard-input*` at call time (the `with-output-to-string (*standard-output*)` capture, its `with-input-from-string` mirror): the bind-activated special rule, per-backend streamArg wiring, the fresh-line string-stream/file rule; plus `*error-output*`, whose designator is the reserved HANDLE 2 (stderr, not `t`) and which `warn` writes through
- [load-inliner.md](load-inliner.md) -- compile-time `load` inlining (`LoadInliner`), `require`/`provide`
- [source-positions.md](source-positions.md) -- `file:line:column` in reader errors (offsets through `LispLexer`/`LispReader`, opening-delimiter reporting) AND in everything the frontend raises after the read (`SourceProvenance`'s cons-keyed side table, the `noteFailure` unwind protocol, the `LispCompileException` boundary), plus the cons-IDENTITY rule every AST pass must honour, the `CompileWarnings` buffer that keeps a retried compile from warning twice, the `rontolisp:current-file`/`current-line` read-time literals, and the compile-path-only divergence with its re-evaluation trigger
- [packed-integer-vectors.md](packed-integer-vectors.md) -- `(unsigned-byte 8|16|32)` rank-1 make-array packs (mask-store / unsigned-read semantics on every backend, `#N@(...)` literal, `%array-alike` subseq lowering, the wasm `TYPE_I8ARR..TYPE_I32ARR` bare arrays + fused aref/aset raw paths + defun inlining, the JVM `long[]` `_iv*` runtime, the `writeHeapType` index>=64 encoding fix, and the `deftype`-alias `:element-type` resolution that made salza2's `'octet` buffers pack -- one resolver, three call sites, plus the two places still alias-blind on purpose; a literal of 16+ elements is baked into the module's static data and copied in by a loop, 4/2/1 bytes an element, with the three traps that emission avoids)
- [dynamic-late-binding.md](dynamic-late-binding.md) -- `--dynamic` late-binding fallback
- [wasi-component.md](wasi-component.md) -- `--component` WASI 0.3 component output
- [wit.md](wit.md) -- `am.ik.wit` WIT parser/printer library + `WitResolver`, the model-based `--emit-wit` emission (`WasiWitDefinitions` + fixtures + generator), the blob-variant renaming, the settled `WitTypeMapper` table (the `result<T,E>` = condition-everywhere decision record), `rontolisp:wit-export` (a world as the authoritative export list) and `rontolisp:wit-import` (calling a WIT interface: the per-backend lowering, the provider decision record, `wit.lisp`)
- [wasm-export-no-wasi.md](wasm-export-no-wasi.md) -- `rontolisp:wasm-export` + `--no-wasi` reactor mode
- [jvm-export.md](jvm-export.md) -- `rontolisp:jvm-export` (typed, Java-callable static methods on a compiled class; the wasm-export twin) + `--no-main` library mode: the type-to-Java mapping and exact-or-throw guards, the string-framing invariant, exports as tree-shaker roots, the top level moving into `<clinit>`, `-o out.jar` (the consumable artifact carrying its own Maven coordinates), and `rontolisp-maven-plugin` (`src/main/lisp` as a source set) over the shared `cli/CompileFrontend` + `cli/JvmSourceCompiler` seam
- [wasm-import.md](wasm-import.md) -- `rontolisp:wasm-import` (host functions callable from Lisp, `:async t` suspending imports as futures) + export `:as` aliases + `--emit-js-glue` (the JavaScript host half, WRITTEN from the declarations) + `--host-boundary` (streaming vs envelope: which of a reactor's bodies leave the JSON envelope, what each shape costs, and why the envelope one lets the build write `defaultHost()` and the whole `worker()`)
- [optimize-dead-code-elimination.md](optimize-dead-code-elimination.md) -- the whole `--optimize` flag: its LEVELS (`--optimize`/`=default` = tree-shaking, WASM + JVM; `=size` additionally declines the wasm-GC speed-for-size emissions), what the wasm shaker drops (functions, types incl. rec-group atomicity, owned data segments, droppable string ranges), the duplicate-body fold (identical bodies emit once; why function identity survives it), the funcall-dispatch gate (and the designator a call site can READ, which never becomes a value in the first place -- written out at the site, or propagated there from the temp an expander bound it to), the dead-typecase-clause pruner and its defgeneric twin (the dispatcher-branch narrowing that dropped 16 of chipz's 18 `%DECOMPRESS` variants), the measured cost of each, and what ROUTING costs a clack module (tiny-routes' cl-ppcre: the CLOS anchoring measurement, the path-template substitution rejected as a default and delivered as the `tiny-routes/lite` opt-in, why the Worker examples build at `=size`)
- [pure-builtin-fold.md](pure-builtin-fold.md) -- the compile-time fold of a PURE built-in over literal arguments (`PureBuiltinFolder`, always on, both compile paths): why the win is reachability rather than arithmetic and how it composes with the print/format literal folds, the curated table with its reason per group, what is deliberately OUT with each exclusion's re-evaluation trigger (float arithmetic, ratio, non-ASCII case, identity, the `floor` family), the four-backend differential harness that decides what may be in the table, the three safety gates (evaluated position only, a user definition wins, a signalling fold declines), the PACKED LITERAL TABLE fold (`coerce`/`make-array` into an `(unsigned-byte N)` vector -- the one fold whose win is bytes and whose result is an object, and why that is still identity-safe), the compile-time WARNING a plain `(defun <cl-function> ...)` now gets (`ClRedefinitionWarnings`: the compile paths keep emitting the standard operator, and say so), and `SourceProvenance.inherit`
- [toplevel-statement-values.md](toplevel-statement-values.md) -- a top-level form is a STATEMENT: both compile paths drop its value, so nothing may be emitted only to be dropped (`compiler/ToplevelStatements`, always on, not behind `--optimize`) -- the constant-valued forms a resolver leaves behind (`in-package` -> a quoted symbol) are deleted, and `defvar`/`defparameter`/`defconstant` are compiled for effect instead of building the name they return; carries the zlib audit that found all 71 of the top-level chunk's string builds were discarded return values (none interned for a registry), what is left in that chunk and why, and the dead `local.tee` the eval mirror used to cost every binding
- [compile-time-boundp.md](compile-time-boundp.md) -- `(boundp 'name)` over a LITERAL symbol is decided at compile time (`compiler/CompileTimeBoundp`, always on, both compile paths): a compiled program is closed, so the answer is which top-level forms before the probe declare the name a global (`GlobalVarCollector` read in declaration order); why the portable `(unless (boundp '+k+) (defconstant +k+ v))` costs twice (it holds the eval runtime open AND hides the definition from the tree-shaker, which is why the fold also runs before the shake in the CLI and the playground), the free soundness gate (`eval`/`load`/`--dynamic`), the straight-line evaluation PREFIX that makes the guard decidable and every position that is not (deferred bodies, loops, conditional definers, lexical `setq`, valueless `defvar`, `special` declarations, `cl` symbols, computed designators), the pre-package-resolution restriction to the "unbound" direction, the zlib row, and why `fboundp` is deliberately out
- [library-defun-pruning.md](library-defun-pruning.md) -- default-on AST pruning of spliced library defuns, rontolisp's own AND ASDF-spliced third-party trees (`LibraryDefunPruner`, `%begin-system` provenance markers, `--no-prune` escape) + `am.ik.jvm` constant-pool deduplication
- [default-run-path.md](default-run-path.md) -- the flagless `rontolisp app.lisp` runs the tree-walking interpreter BY DECISION: the measured cost table (20x-240x off SBCL on loops, and why that stands), where the fixed compile cost really goes (compiler class-load/JIT warm-up on a cold JVM, NOT the splice chain -- 4 ms warm), why compile-by-default and the source-hash cache lost (a closed-world native image cannot define classes at run time; an engine auto-switch makes every open interpreter/compile-path divergence a silent run-to-run change), and the re-evaluation triggers (GraalVM Crema executing at compiled speed, the divergence backlog closing)
- [emitted-output-determinism.md](emitted-output-determinism.md) -- the same program must compile to the same bytes on every run: the `Map.of`/`Set.of` salted-iteration trap (the type lattice leaking into `%SUBTYPEP-ANCESTOR-TABLE`), why native-image + `CiSpecE2eTest` are blind to it, the rule for any table whose order reaches output, and the runtime sibling -- a host `toString` reaching a printer prints an identity hash
- [random.md](random.md) -- `random` is a PSEUDO-random draw from a generator inside the program on every backend, never a host call per draw: `ThreadLocalRandom` on the interpreter/JVM (not `Math.random()`, whose shared `AtomicLong` CAS cost 20 of its 24 ns), an inline SplitMix64 over `RANDOM_STATE_ADDR` on every wasm build, lazily seeded ONCE per instance from `random_get` at the call site (and why the seeding is there and not in a `_start` prologue); the decision that `*random-state*` stays a no-op, why `--host-random` now means "seeded by the host" rather than "a host call per draw", and the rule that keeps `rontolisp::%random-byte` on real entropy
- [no-gc-scalar-wasm.md](no-gc-scalar-wasm.md) -- `--no-gc` non-GC scalar WASM backend
- [wasm-callable-arity.md](wasm-callable-arity.md) -- the 10-parameter wasm callable limit, why it is an index ORIGIN that cannot simply be raised, and the three ways past it: bundling a too-wide defun's surplus parameters, giving an 11..14-argument call site its own dispatcher APPENDED after the fixed block (derived from the program, byte-identical when unused, zlib -7.8%), and spreading anything wider through `apply`'s SPREAD dispatcher (why a `&key` call site reaches the limit at seven parameters, why the pass must run before the `usesEval` scan, why the map family shares the ceiling, and the `freshCtx` trap)
- [wasm-function-body-size.md](wasm-function-body-size.md) -- no single emitted WASM function body may grow without bound: wasmtime's cold compile memory is superlinear in ONE body's size (850 KB -> 25.8 GB), the default compilation cache hides it, and `ulimit -v` cannot bound it
- [wasm-gc-strings.md](wasm-gc-strings.md) -- WASM GC-backend strings as `$str_bytes` arrays (HEAP_PTR as a stack pointer, `_str_fresh` counter ids, `_str_to_mem`/`_write_str_gc` bridges) -- retires the linear string heap leak
- [wasm-gc-final-types.md](wasm-gc-final-types.md) -- every emitted wasm-GC type must be `sub final`: wasmtime 47+ lowers `ref.cast`/`ref.test`/`call_indirect` against a final target to one inline type-id compare, while <= 46 pays an `is_subtype` libcall into the engine-global registry lock on every FAILING concrete test -- which is what collapsed concurrent `wasmtime serve` throughput ~15x (the 0x4F/0x50 enum swap history, the bare-comptype spelling, the 46-vs-47 serve measurements, `RecTypeDefTest`)
- [wasm-shortest-encoding.md](wasm-shortest-encoding.md) -- every wasm byte the project emits is in its shortest LEGAL encoding, with `wasm-tools parse (wasm-tools print M)` == `M` as both the definition and the pinning test: the one-byte nullable-abstract reference, `u32` (not s32) for every index/count/length, the bare-comptype `sub final`, no empty or repeated-adjacent sections -- and the tree-shaker's blocktype predicate, which has to decode every short form the writer can produce
- [wasm-gc-heap-pregrow.md](wasm-gc-heap-pregrow.md) -- `_start` pre-grows the engine's GC heap with one dropped allocation, sized from the emitted code (16x it, clamped to 16-64 MiB; **1 MiB in serve mode**, where it is paid per instance and costs ~1.5 ms/MiB of request latency): wasmtime's copying collector otherwise never grows past ~2x the live set, so a loaded library stack makes every allocation-heavy hot loop pay a whole-live-set copy per few hundred KB (the todo-188 "module-size tax") -- and on wasmtime 47 it is a CORRECTNESS knob too, because that collector loses a live reference when it runs during an exception unwind (todo-409: green under `-C collector=drc`, `cast failure` under the default `copying`); `WasmGcHeapPregrowTest`
- [wasm-shared-coercion.md](wasm-shared-coercion.md) -- the numeric-to-`f64` coercion is ONE runtime function (`_as_f64`), never an inlined ladder: why a five-way `ref.test` chain per OPERAND was 43% of a float program's code section (and half of it inside the ratio runtime's own copy), the measured -34%/-10% it bought, why it sits at the DEFAULT optimize level rather than behind `--optimize=size`, and the fast-path-not-inlining re-evaluation trigger
- [string-write-runtime.md](string-write-runtime.md) -- the string arm every rank-1 `(setf (aref v i) x)` / `(setf (elt v i) x)` / `(setf (char s i) c)` carries is ONE spliced defun (`%schar-set-runtime`), never an inlined subseq/concat rebuild: the ~8 KB per site an array-only program used to pay (cube: 203 of 218 KB), why the helper spells `%subseq-core` rather than `subseq`, the deliberately generous injection gate, and the one-site crossover
- [subseq-runtime.md](subseq-runtime.md) -- `subseq` and the general-array element access are shared callees, never per-site code: the spliced `%subseq-runtime` defun (2,316 -> 11 bytes a site; injected by the BACKEND, beside the builtin wrappers, because that is where most `subseq` sites live) and the wasm `_arr_get`/`_arr_set` displacement walk (five accessors, two temps each), the ONE `programUsesGeneralArrayOp` gate the JVM shares with its array runtime and why wasm gates differently, and the -4.5%..-36% it took off every checked-in browser artifact
- [seq-conversion-runtime.md](seq-conversion-runtime.md) -- the literal sequence conversions (`coerce` to `'list`/`'string`/`'vector`, i.e. the dispatch every generic sequence lowering wraps its scan in) are the shared `%seq-to-list`/`%seq-to-string`/`%seq-to-vector` trio, never per-site `map` loops: one routing point at the compilers' `coerce` case serves `reverse`/`remove`/`position`/`count`/`sort`/... sites and the wrapper catalog alike (8-10 KB -> 66-852 bytes a site), the `programUsesSeqConversion` injection gate beside `%subseq-runtime` with the same JVM array-gate asymmetry, and the -9%..-51% it took off every sequence-using module (minesweeper -45%, the un-shaken wrapper catalog halved)
- [sequence-op-runtimes.md](sequence-op-runtimes.md) -- `replace` / `fill` / `map-into` are shared callees, never per-site code: the `%replace-runtime` / `%fill-runtime` / `%map-into-runtime-<n>` helpers with the keyword bounds as nil-defaulting PARAMETERS (3,806/1,718/1,949 -> 21/15/17 bytes a site, `CHIPZ::UPDATE-WINDOW` 18 KB -> gone, zlib -13..-19%), why `map-into` needs one helper per source count, the injection order the `%subseq-runtime` gate depends on, the `replace`-into-a-LIST arm the compile paths never had (JVM `ClassCastException`, WASM text garbage, `(setf (subseq l ...))` a silent no-op) and why the shared runtime is what made it affordable; then todo-325's ARM NARROWING -- the `%arrayp` arm as its own callee that a site with a provably-array destination calls directly (`%REPLACE-RUNTIME` 4,463 -> gone on zlib, another -6.7%), the cursor-walk list SOURCE that made it worth doing, and the soundness argument for the gate (what a wrong answer costs, and why `Ctx.arrayLocals` carries a weaker fact than a representation kind) -- plus the measured `_dispatch_spread` finding: NOT an over-approximating scan but the call-site arity ceiling, since fixed in [wasm-callable-arity.md](wasm-callable-arity.md) for another -7.8%
- [sort.md](sort.md) -- `sort` is ONE merge sort, shared by every backend and by `stable-sort`: the `%sort-runtime` defun both compile paths call and the arm-for-arm Java twin `LispEvaluator.sortValues` runs, replacing the insertion sort (interpreter) and selection sort (JVM, wasm) that made `bench-report/`'s 400,000-element `sort` the one cell timing out on all three rontolisp backends (now 1,706 / 471 / 487 ms); the split and tie rule that make the four backends answer ONE permutation for one input (stable, but promised nowhere), what the argument list looks like afterwards and why that still differs, the array-gate-free injection gate, and the wasm name-registry arming a site must do itself because the helper body cannot
- [length-runtime.md](length-runtime.md) -- the generic `length` dispatch is a shared callee, never per-site code: the JVM always had `_length` (one `invokestatic` a site), the WASM backend inlined the whole sequence-type ladder at every generic site until todo-331 (~300 bytes each, 66 copies and 13.6% of the zlib module) and now emits one `call _seq_len` (zlib `--optimize=size` -8.1%), the `DeclaredArrayTypes` packed-vector short-circuit that stays a direct `array.len` at the site, why the call is at the DEFAULT level (inflate-loop timing unchanged), and the fast-path-not-inlining re-evaluation trigger
- [wasm-int-fusion.md](wasm-int-fusion.md) -- WASM integer expression-tree fusion: nested `+ - * mod rem logand/ior/xor lognot ash` trees keep raw-i64 intermediates and box only at the root, with per-leaf guards + per-op overflow checks bailing to a generic-fallback recompute (never a semantic variant; the `_fx_*` helpers, the strength reductions, the masked-wrap peephole, flet-lambda/defun call substitution, the cached-`t` `_t_sym` helper, the non-trigger conditions, and the `--optimize=size` gate)
- [wasm-counted-loops.md](wasm-counted-loops.md) -- a loop's induction variable as a bare `i64` counter with no boxed shadow: `dotimes` over a LITERAL bound (`WasmDotimesCompiler`) and the `let` + `while` sandwich `loop`'s numeric `for` head lowers to (`WasmCountedLoopCompiler`). The `i64.ge_s` exit / `i64.add` step emitted instead of a generic `<` call plus a re-box per iteration, the counted `RawLocal` flavour and the five sites that branch on it, the two eligibility scans that are the whole soundness proof (which place arguments of which modify operator write a VARIABLE; how a `while` test BOUNDS a variable's range), why it is at every optimize level rather than behind `--optimize=size`, and the non-literal-bound widening question
- [wasm-unboxed-locals.md](wasm-unboxed-locals.md) -- WASM unboxed (dual-representation) `let` locals: an i64 slot + a boxed shadow slot ("shadow non-null = use the shadow"), raw stores on the fused fast path with a total boxed escape (overflow promotion, floats), on-demand `ref.i31` boxing for non-fused reads, the second locals run whose references are emitted as placeholders and spliced out (`buildLocalsAndPatch`), the eligibility rules, and the `--optimize=size` gate it shares with fusion
- [jvm-int-fusion.md](jvm-int-fusion.md) -- JVM integer expression-tree fusion: nested `+ - * mod rem logand/ior/xor lognot ash` trees compile into OUTLINED, structurally-shared `_fx$N` methods that keep raw-`long` intermediates and box only the root, with per-leaf `instanceof Long` guards and an `ArithmeticException` overflow region bailing to a generic-helper recompute (never a semantic variant; the masked-wrap peephole, fused raw comparisons for `if`/`while` heads, defun/flet-lambda substitution, packed-aref raw leaves, the unboxed dual-representation `let` locals with their `_ubSentinel` shadow, and the `--optimize=size`/`--dynamic` gates)
- [jvm-bignum-literal-pool.md](jvm-bignum-literal-pool.md) -- JVM bignum literals: one `java.math.BigInteger` instance per distinct value in a `_bi$N` static field built in `<clinit>`, so a use site is a `GETSTATIC` rather than a decimal-string parse plus an allocation; the `<clinit>` gate and name/descriptor fallback for a program whose only initializer reason is a literal, the initializer order and `max_stack`, why the field carries no attribute, and the ironclad SHA-512 measurement
- [jvm-double-arithmetic.md](jvm-double-arithmetic.md) -- JVM double-float arithmetic: the `hasDoubleLiteral` routing predicate, the unboxed IEEE operand emission (raw literals, no box between interior nodes, `_dbl` answering an existing `Double` as-is), the all-Double second fast path inside the fused `_fx$N` methods and its strict per-leaf guards, why a float-initialised `let` binding declines the integer dual representation, `setq` in statement position, and the float AND RATIO arms `_mod`/`_rem` were missing (a ratio through the `_ratnum`/`_ratden` prologue, sharing `_mod`'s divisor-sign correction)
- [jvm-typed-loops.md](jvm-typed-loops.md) -- JVM typed numeric loops: a `dotimes` over packed float arrays (fixnum index math, `aref`/`setf aref`, `let` temporaries, `+ - * /`, the unary Math functions, binary comparisons under `if`/`when`/`unless`, nested `dotimes`) compiles to primitive `long`/`double` locals and raw `float[]`/`double[]` accesses behind ONE guard at entry (free variables speculated as array / int-bounded Long / strict or Long-accepting Double, the magnitude bound that keeps fixnum arithmetic exact, the single/double array variants, the exception write-back of assigned accumulators) with the ordinary `expandDotimes` emission as the bail target -- never a semantic variant; the subset, what is still rejected, the `--optimize=size` gate, and the llama2 numbers it bought
- [wasm-bignum.md](wasm-bignum.md) -- WASM GC-backend exact integers in three tiers (i31 fixnum, `TYPE_BIGNUM` `{i64}`, `TYPE_BIGINT` two's-complement 32-bit limbs), the always-narrowest-tier normalization invariant, the `_limb_*`/`_big_*` runtime (overflow-promoting arithmetic, bitwise, divrem, print, read, eq/hash) -- exact at any magnitude, as CL requires; deliberate limits: i32 ratio components, long-clamped float->integer conversion, i64-range isqrt/random
- [time-environment-builtins.md](time-environment-builtins.md) -- time/environment built-ins, plus the environment-ENQUIRY family (`lisp-implementation-*` / `software-*` / `machine-*` / `*-site-name`): why every answer is a constant, why the host is never consulted even where it could be, and the single `%target-machine-type` per-backend piece
- [fetch-http.md](fetch-http.md) -- `rontolisp:fetch` outgoing HTTP (the `FetchResponseShape` result plist; `:body` a stream of OCTET chunks on every backend, `read-all` decoding once at the drain, so a relayed reply is byte-exact -- todo-370), the `--host-fetch` reactor transport over `env.fetch`, the reply-body split out of the envelope and what the DEFAULT boundary (`envelope`) drops, and the `rontolisp:http-handler` transports
- [http-server.md](http-server.md) -- the server-side HTTP value model since the Clack cutover: a handler receives the Clack environment (`ClackEnv`, 15 keys) and returns the Clack response; shape declared once, construction native per backend (with the measurements that pinned that division), the two `:raw-body` modes and the bivalent buffered body stream, the response normalizer contract (bare string refused, delayed form only), the byte-exact `(unsigned-byte 8)` response body and octet-chunk STREAM body (carried, never rendered -- and the one reactor-envelope arm that renders octets as lenient UTF-8 text, with the reason), the `HttpServerLibrary` splice + its module-size filter
- [async-await.md](async-await.md) -- async-defun/await, futures and asynchronous streams
- [error-handling.md](error-handling.md) -- unwind-protect (interpreter try/finally, JVM exception tables + return-escape cleanups), condition objects (define-condition over the CLOS subset, seeded hierarchy, error/signal/warn designators, with-slots), handler-case/ignore-errors (typed catching, ThreadLocal condition/depth channels; only --no-gc rejects catching), the RESTART system (handler-bind handlers at the signal point + the two global stacks + restart-case over catch/throw, one shared lowering on all four backends; a handler-case pushes a cluster of its own, so it SHADOWS an enclosing handler-bind, and `signal` declines a handler-case no clause of which matches -- at the signal point, on all four backends), the condition REPORT as the one text a condition both signals and prints (`%condition-report-str` on the print-object seam), the wasm-GC lazy signal messages (message operands never compiled where no program code can hold a condition; `%no-applicable-method` signals a seeded typed condition instead of rendered prose), and the UNCAUGHT report -- the one `Unhandled condition: ...` line all four backends write before exiting, and what it costs the wasm-GC gates to have it
- [concurrent-served-requests.md](concurrent-served-requests.md) -- the one-virtual-thread-per-request rule for shared interpreter/JVM state (the stream table, the global namespaces, every lazy library load), the bug family it produced and the shape a new lazy load must follow
- [mutexes.md](mutexes.md) -- `rontolisp:make-mutex`/`mutex-acquire`/`mutex-release`/`with-mutex` (a `ReentrantLock` on the interpreter and the JVM, no-ops on WASM; the handle is opaque and differs per backend) and the `bordeaux-threads` shim system + the per-backend `Features` now threaded into every shim source
- [threads.md](threads.md) -- `rontolisp:make-thread`/`join-thread`/`threadp`/`thread-alive-p`/`destroy-thread` (real virtual threads on the interpreter and the JVM, call-time errors from the WASM shim defuns; the handle is opaque), the bindings-alist dynamic binding in the spawned thread (JVM `_dtl` runtime name dispatch + the forced special set) and the `bt2` package half of the bordeaux-threads shim (clack's handler.lisp is the driving consumer)
- [tcp-sockets.md](tcp-sockets.md) -- `rontolisp:tcp-*` TCP sockets, `rontolisp:tls-connect`/`tls-upgrade`/`tls-listen` (stream-handle integration + the wasi:sockets / wasi:tls@0.3.0-draft component variants -- client TLS runs under `--component`, TLS servers never will), the `usocket` compatibility shim (usocket.lisp + the built-in ASDF system, incl. the `tcp-set-timeout`-backed `socket-option :receive-timeout` and the `listen`-poll `wait-for-input` decisions) and the client-side `cl+ssl` shim over `tls-upgrade`
- [mito.md](mito.md) -- Mito (DAO + schema migration) end to end: how the chipz CRC32 SLICE override was retired once its own widening trigger fired, what the migration workflow needed from the core language, why `generate-migrations` is interpreter+JVM only (and the tenth-preview1-import trigger that would close it), the three UPSTREAM defects reproduced faithfully (bare `:references`, sxql's alter-table bind loss, the pg_attrdef join), why `count-dao` and every sxql SQL FUNCTION operator are interpreter-only (the `find-symbol` deviation, `.todo/254`), the `MitoE2eTest` + bilingual-guide coverage, and the SCRAM/authentication-timeout caveat every DB E2E must respect
- [lack.md](lack.md) -- the lack request/response + middleware ecosystem: what loads verbatim, the six general language/runtime gaps it closed (short-form method combinations, `:if-exists :append`, `uiop:with-temporary-file`, flexi-streams' real in-memory Gray streams, keyword `:conc-name`, `*package*`-derived symbol construction), and the four general fixes that took the chain to all four backends (JVM branch relaxation, deftype-alias `concatenate` result types, the babel package redirect, redefined-defun emission)
- [clack.md](clack.md) -- Clack support: the late-bound `clack-handler-rontolisp` shim system (why its package is NOT seeded, the dotted system-name alias, the eager compile-path splice), the stoppable `rontolisp::%http-server-*` seam behind run/stop, why there is NO env/response bridge any more (the app IS the handler, `.kb/http-server.md`), the ONE-clackup-source rule (`:server :rontolisp` picks the transport per target at read time -- socket, `http-handler` directive, or the `#+rontolisp-reactor` marker leg -- and WHY it is a reader feature), the nested `http-handler` detection for the component leg, the Preview-1 call-time-error policy extensions, and the shared host-driven-reactor transport `http-reactor.lisp` behind BOTH designators (`clack-handler-reactor` = host-driven on every backend; the JSON envelope as an API, why it catches, and why the designator names the TRANSPORT rather than a deployment vendor -- the 2026-08-09 rename away from `:cloudflare-workers`, alias-free), the head/body-source and body-sink halves of that envelope and the `--host-boundary` choice between carrying a body in it or beside it, plus the TWO verified routing layers (tiny-routes and ningle) and what makes ningle a different test rather than a second route table
- [json.md](json.md) -- `rontolisp:json-parse`/`json-stringify` Lisp-source library + splice
- [linalg.md](linalg.md) -- `linalg` package (numpy-style vector/matrix ops) Lisp-source library + the standard array functions
- [torch.md](torch.md) -- `torch` package (PyTorch-style tensor + reverse-mode autograd over the linalg kernels): the defun-only pruning decision, the six-slot tensor record, the adjoint table + shared gradcheck, the no-grad macro's three seams, the five-slot MODULE record whose fields plist is its parameter registration (and why there is no `torch:call`/ReLU module/`ModuleList`), the OPTIMIZER record whose fields plist is its state and whose `torch:step` updates in place off the tape (and why the step counter increments first), the `DataLoader`-free batching/mask functions, the four-backend pins, and the whole-package acceptance example (`examples/llm-from-scratch/`, the book's Transformer chapter)
- [vec.md](vec.md) -- `vec` package + the packed float-array type and its four `--simd` acceleration layers
- [linalg-simd.md](linalg-simd.md) -- `--simd` interception of the `linalg:` kernels (the declined-input protocol, the precision contract)
- [simd-parallel.md](simd-parallel.md) -- `--parallel`: the `--simd` matrix products (`vec:matvec`/`-into`, `linalg:dot` over a matrix, the stacked `matmul`) over a row range per thread -- the row-independence invariant that keeps them bit-identical and excludes every reduction, the `RONTOLISP_THREADS` knob and why the flag must be explicit, the caller-splits-and-spins dispatch shape the five probes chose over `pool.invoke`, the 2^17 threshold and 2^13 grain, the `--gpu` below-the-device rule, the llama2 and GEMM numbers
- [linalg-blas.md](linalg-blas.md) -- `--blas`: the `linalg:` matrix product on a tuned CBLAS from the OS (finding one, refusing an untuned one, the chain above `--simd`)
- [gpu.md](gpu.md) -- `--gpu`: `am.ik.gpu`, the language-independent library that runs a member on a GPU or declines (the never-throwing invariant, the pure-FFM CUDA and Metal bindings behind one sealed `GpuDevice` seam, the checked-in PTX and its regeneration command, the CUresult table and the sticky-error rule, the three-calls-in-order rule that makes an out-of-memory decline cost the device nothing, the critical-copy rule and its two safepoint bounds); every threshold and the measurement that fixed it; the three kinds of precision break and the tolerances that pin them; DEVICE RESIDENCY -- the weakly-keyed identity cache, the two seams every in-place write and every host read must report through, lazy results and the stub with no host array, the tiers that exist only over a resident operand, the GEMV's "offered twice, never written" rule, and the collector the mode depends on (and does not, on Apple); the two backends' own sections -- the register-tiled f32 GEMM and the launch pipeline on CUDA, MPS / the buffer pool / software binary64 / the subnormal flush guard / the idle clock and the measured decline of lazy results on Metal; and the interception layer -- the intercepted set, the install-order chain `device -> --blas -> --simd -> defun` and why the device goes on top, the three-attempt call site, the `.wasm` refusal, the `-Pweb` substitution, why the emitted class carries `am.ik.gpu`'s own class files rather than a flattened copy, the test map and the native-image registrations
- [vec.md](vec.md) -- `vec:` package (packed-f64 vector kernels over the packed float-array type) + JVM `--simd` (jdk.incubator.vector) and `--no-gc` native `v128` acceleration
- [map-family.md](map-family.md) -- `mapcar`/`mapc`/`mapcan`/`maplist`/`mapcon`/`mapl` over ANY number of lists on all four backends: the shared `expandMapFamily` lowering, the per-backend inline emitters (and the `indirectCallArities` trap), the one `mapFamilyWrapper` value shape, the guards, and the `append`-not-`nconc` divergence
- [concatenate-result-families.md](concatenate-result-families.md) -- `concatenate`'s `string`/`list`/`vector` result families: the one shared designator normalizer + compile-path lowering (`compiler/ConcatenateForms`), deftype-alias designators resolved through the class registry, the `%seq-string` string-argument normalizer, the reference-gated `#'concatenate` wrapper, the `--no-gc` restriction, and `coerce`'s share of the packed arm (todo-319) with the `map` / computed-designator triggers that are left
- [adjustable-arrays.md](adjustable-arrays.md) -- fill-pointer / `:adjustable` / displaced arrays + `vector-push`/`-pop`/`-push-extend` + `adjust-array`/`array-displacement` (all four backends); also the JVM runtime-helper GATES (array/hash/eval), which are verified against the emitted bytecode rather than predicted from a source scan
- [url.md](url.md) -- `rontolisp:url-*`/`query-param*` URL / query-string Lisp-source library
- [java-interop.md](java-interop.md) -- `java:` reflection interop bridge
- [ffi.md](ffi.md) -- `ffi:`: the foreign primitives upstream CFFI's `cffi-sys` backend is written over (`am.ik.ffi` -> `eval/FfiInterop` -> `eval/FfiBridge`, the `objc:` shape): calling conventions decided at run time from CFFI keyword designators, `LispForeignPointer` as its own value, `ffi:alloc` IS `malloc`, unbound downcall handles cached by shape, per-thread `errno` capture, the `:varargs` marker, never-throwing upcalls, by-value structs with the C padding rule, the WASM refusal by name and the web-build cut
- [cffi.md](cffi.md) -- upstream CFFI runs here from its own source: the three bundled pieces (the `cffi-sys` backend, the replacement `cffi.asd`, the `strings.lisp` substitute) and nothing else, the flat namespace the backend keeps by hand, structures by value with no libffi, the lazy process handle that keeps loading free of native access, the `cffi-grovel`/`cffi-libffi` refusals, and the leaf-module package bracket the backend file needed
- [objc.md](objc.md) -- `objc:` + `appkit:`: a native macOS window from the REPL through FFM (`am.ik.objc`, no reflection, so the native binary too): thread 0 owns AppKit and the native binary hands it over at startup (the launcher's `ParkEventLoop`, re-entered), every verb hops through one trampoline upcall with the inline re-entrancy rule, `objc:send`'s shape is DERIVED from the selector's type encoding and served from a CLOSED registered table in the binary, the project's first `foreign.upcalls`, one retain per wrapper released on thread 0, the JVM class output that carries the binding as an embedded blob and calls back into the program (definition order matters: the verifier wants the `catch` type first), and the tests that pin each
- [template-class-embedding.md](template-class-embedding.md) -- when/how to embed a Java "template" class, and when to embed a CLOSURE of them instead of a hand-kept copy
- [stackmap-augmenter.md](stackmap-augmenter.md) -- class version 61 via `am.ik.jvm.StackMapAugmenter` (offline StackMapTable computation over frame-free emitter output; pipeline order, merge rules, size cost, what v61 unlocks)
- [jvm-osr-backedges.md](jvm-osr-backedges.md) -- no backward branch in an emitted class may target a bci with a NON-EMPTY operand stack: HotSpot only enters an OSR compilation at an empty-stack backedge, so a loop head under the enclosing expression's pending operands is refused at every tier and a method entered once (every top-level form, every `defun` called once) never compiles at all -- the `nth` walk that cost 15.4 s against wasm's 1.9 s, the two emitter shapes that satisfy it (hoist into a method / spill the operands with `JvmEmitHelper.inLoopScope`), the escape-path `SpillScope` bookkeeping, the un-spillable `new` case, and why the check is a corpus test rather than a compile-time throw
- [jvm-method-size-limits.md](jvm-method-size-limits.md) -- no emitted JVM method may outgrow the 64 KB code limit (a branch past the signed 16-bit offset relaxes to `goto_w` via `am.ik.jvm.BranchRelaxer`): the loud guard, the segmented `_invoke_N`/`_lookup` dispatchers, chunked data-table forms, symbol funcall designators, the method-size debug flag
- [eval-runtime.md](eval-runtime.md) -- runtime `eval` interpreter embedded in compiled output
- [hash-tables.md](hash-tables.md) -- hash table representation per backend, the `equalp` key fold, and the `#<HASH-TABLE :TEST ... :COUNT n>` print syntax the four of them must agree on
- [documentation-site.md](documentation-site.md) -- doc site layout, code-fence conventions, the two-tier client-side search index, build/preview, and the generated agent skill
- [formatter.md](formatter.md) -- `rontolisp format` (the source formatter): the whitespace-only invariant (identical token stream + fixpoint, pinned over every checked-in `.lisp`/`.asd`), why it has its own lossless CST reader instead of `LispReader`, the `Style` model and the `childStyle`/`statements` fields that carry what structure cannot, the two width mechanics that silently overrun the margin when forgotten (closing-paren accounting; "would moving it help"), the naming-convention guess for unknown macros, and the three deliberate divergences from trivial-formatter with their reasons
- [dists.md](dists.md) -- the download half of `ql:quickload`: `eval/DistClient` over a LIST of Quicklisp-format distributions, Ultralisp opt-in through `ql-dist:install-dist` / `--dist` / `RONTOLISP_DISTS`, the per-system first-dist-wins search order (and how naming quicklisp reorders it), lazy per-dist index loading, the name-is-the-cache-directory identity rule, `ql:update-dist`, and the compile-path directive handling
- [asdf.md](asdf.md) -- `asdf:defsystem`/`asdf:load-system`/`asdf:test-system` limited ASDF subset (.asd parsed as data, LoadInliner splice + interpreter runtime), the runtime component metaobjects (`asdf:find-system` -> a memoized `asdf:system` instance, real classes + readers on every backend, asdf.lisp), `:class :package-inferred-system` (graph derived from each file's `defpackage`), the shim/replacement-`.asd`/leaf-module substitution ladder, the `rontolisp test` rove runner (generated program, verdict read from `*last-suite-report*`, exit contract on all four backends), every loadable real library incl. the ironclad slice, and WHY this is a shim rather than upstream ASDF (the 2026-08-18 spike: it loads, but the port surface, the 2.85 s startup and our print-keyed hash tables decided against it)
- [uiop.md](uiop.md) -- uiop as the 15-sub-package bundle upstream is: the checked-in `uiop-exports.txt` inventory (435 rows / 429 symbols) that `PackageRegistry` and `eval.UiopLibrary` are both built from, the HOME-spelling canonicalization every pass has to match, the `not-implemented-error` stub synthesized per definition kind, and `UiopCoverageTest` -- the gate items 354-365 report to
- [declarations-type-checks.md](declarations-type-checks.md) -- `declare`/`declaim`/`proclaim`/`the` value-semantics no-ops, `eval-when` (+ top-level flattening), `check-type`/`assert`, shared type-specifier tests; PLUS declaration-driven array emission on the wasm-GC backends (declared/slot-`:type`/init-derived array kinds -> single-arm rank-1 `aref`/`%aset`/`length` behind a trapping cast, the size-level `%aset` operand hoist, the defaulted all-`&optional` deftype registration, and the todo-320 ceiling/arithmetic measurements)
- [flet-labels.md](flet-labels.md) -- `flet`/`labels` local functions via let-bound lambdas + Lisp-2 call-site rewrite (labels = nil-then-setq letrec)
- [symbol-macrolet.md](symbol-macrolet.md) -- `symbol-macrolet` via one shared shadow-aware substitution (setq→setf write-through, user-macro hook seam), and the global `define-symbol-macro` on the same walk (interpreter table + the compile paths' drop-and-substitute, `cffi:defcvar`)
- [dynamic-special-variables.md](dynamic-special-variables.md) -- dynamic (special) variable binding (`defvar`/`declaim special` + `let`/`let*`/`progv`): thread-scoped shallow binding on the interpreter, static-field/module-global save/restore on the compilers
- [multiple-values.md](multiple-values.md) -- `values`/`multiple-value-bind`/`-list`/`-call`/`nth-value` as a syntactic lowering (no runtime representation); floor-family + gethash secondary values, floor-family divisor

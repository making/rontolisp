(defun build ()
  (let ((a (make-array 1000000)))
    (dotimes (i 1000000) (setf (aref a i) (1+ i)))
    a))
(defun run (a n)
  (let ((s 0))
    (dotimes (i n) (setq s (+ s (aref a (random 1000000)))))
    s))
(print (run (build) 10000000))

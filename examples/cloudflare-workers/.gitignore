# Build products and local tool state, for every Worker project here.
# Every .wasm is produced by the directory's build.sh; dist/ is jco's output.
*.wasm
dist/
node_modules/
.wrangler/
package-lock.json

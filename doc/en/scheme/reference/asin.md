# asin

`(asin z)`

Returns the arcsine of `z`, in radians. `(asin 0)` is the exact `0`. There are no complex numbers: an argument outside [-1, 1] is refused with an error naming `asin`.

```scheme
(asin 1) ; => 1.5707963267948966
(asin 0) ; => 0
```

# remainder

`(remainder n1 n2)`

The remainder of `quotient`: its sign is that of the dividend `n1`. The same as `truncate-remainder`.

```scheme
(remainder 17 5) ; => 2
(remainder -17 5) ; => -2
```

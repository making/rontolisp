# sin

`(sin z)`

Returns the sine of `z`, in radians. `(sin 0)` is the exact `0`; any other argument gives a flonum, with the same digits on every backend.

```scheme
(sin 0) ; => 0
(sin 1) ; => 0.8414709848078965
```

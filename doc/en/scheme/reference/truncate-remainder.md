# truncate-remainder

`(truncate-remainder n1 n2)`

The remainder matching `truncate-quotient`; its sign is that of `n1`.

```scheme
(truncate-remainder -7 2) ; => -1
(truncate-remainder 7 -2) ; => 1
```

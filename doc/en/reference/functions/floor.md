# floor

`(floor number)`

Rounds `number` toward negative infinity to an integer. In rontolisp it takes a single argument and returns a single integer value (no optional divisor and no second remainder value as in full Common Lisp).

```lisp
(floor 3.7) ; => 3
```

```lisp
(floor -3.7) ; => -4
```

# geom:surface-area

`(geom:surface-area solid)`

The total area of the solid's mesh triangles. See the [solid modeling guide](../../guides/solid-modeling.md).

```lisp
(geom:surface-area (geom:box '(100 200 300)))
; => 220000.0
```

# float-radix

`(float-radix float)`

The radix of the float representation. Every float here is a binary double, so the answer is always `2`. The argument is evaluated once.

```lisp
(float-radix 1.0) ; => 2
```

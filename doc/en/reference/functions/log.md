# log

`(log number)`

Returns the natural logarithm (base e) of `number` as a float. Only the one-argument form is supported -- there is no `(log number base)` form for an arbitrary base. Available on the interpreter and JVM backends only (not WASM).

```lisp
(log 1) ; => 0.0
```

# ceiling

`(ceiling number)`

Rounds `number` toward positive infinity to an integer. In rontolisp it takes a single argument and returns a single integer value (no optional divisor and no second remainder value as in full Common Lisp).

```lisp
(ceiling 3.2) ; => 4
```

```lisp
(ceiling -3.2) ; => -3
```

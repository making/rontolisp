# /

`(/ z)` `(/ z1 z2 ...)`

引数が 1 つなら逆数を、2 つ以上なら最初の引数を残りで左から順に割った値を返します。正確な整数どうしの割り算は、割り切れなければ正確な分数になります。正確なゼロ除算はエラーになり、浮動小数点数を含む割り算は IEEE 754 に従います（`(/ 1 0.0)` は `+inf.0`）。

```scheme
(/ 1 3) ; => 1/3
(/ 6 3) ; => 2
(/ 7.0 2) ; => 3.5
```

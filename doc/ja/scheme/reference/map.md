# map

`(map proc list1 list2 ...)`

`proc` をリストの各要素に順に適用し、結果のリストを返します。リストを複数渡すと `proc` はリストごとに 1 つずつ引数を受け取り、結果の長さは最も短いリストに揃います。

```scheme
(map (lambda (x) (* x x)) '(1 2 3)) ; => (1 4 9)
(map + '(1 2 3) '(10 20)) ; => (11 22)
```

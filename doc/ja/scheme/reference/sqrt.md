# sqrt

`(sqrt z)`

`z` の平方根を返します。平方根が正確に表せる正確数の引数では正確な結果になり（`(sqrt 16)` は `4`、`(sqrt 1/4)` は `1/2`）、それ以外では浮動小数点数になります。複素数はないため、負の数の平方根は `sqrt` を名指しするエラーとして拒否されます。

```scheme
(sqrt 16) ; => 4
(sqrt 1/4) ; => 1/2
(sqrt 2) ; => 1.4142135623730951
```

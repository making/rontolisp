# char-ready?

`(char-ready?)`

現在の入力ポートからブロックせずに文字を読めるなら `#t` を返します。この実装では常に `#t` です。データが待っているときと入力の終わりでは R7RS どおりの答えですが、まだ何も入力されていない端末でも `#t` になり、その後の読み取りは入力を待ちます。ポート引数はありません。

```scheme
(char-ready?) ; => #t
```

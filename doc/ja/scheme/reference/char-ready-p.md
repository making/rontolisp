# char-ready?

`(char-ready? [port])`

現在の入力ポートからブロックせずに文字を読めるなら `#t` を返します。この実装では常に `#t` です。データが待っているときと入力の終わりでは R7RS どおりの答えですが、まだ何も入力されていない端末でも `#t` になり、その後の読み取りは入力を待ちます。`port` を渡すとそのポートについて答えます。`port` は開いているテキスト入力ポートでなければならず、答えはやはり `#t` です。

```scheme
(char-ready?) ; => #t
```

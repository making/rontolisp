# cdr

`(cdr pair)`

`pair` の cdr を返します。リストなら最初の要素を除いたリストです。

仕様との差異: `car` と同様に、`(cdr '())` はエラーにならず `()` を返します。

```scheme
(cdr '(1 2 3)) ; => (2 3)
(cdr '(1 . 2)) ; => 2
(cdr '((a) b c)) ; => (b c)
```

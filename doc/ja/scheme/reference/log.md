# log

`(log z)` `(log z base)`

`z` の自然対数を返します。引数が 2 つなら `base` を底とする `z` の対数を返します。`(log 1)` は正確数の `0`、`(log 0)` は `-inf.0` です。複素数はないため、負の数の対数は `log` を名指しするエラーとして拒否されます。

```scheme
(log 1) ; => 0
(log 100 10) ; => 2.0
(log 0) ; => -inf.0
```

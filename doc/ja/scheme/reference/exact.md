# exact

`(exact z)`

`z` と等しい正確な数を返します。浮動小数点数はその 2 進表現の正確な値に変換されるので、`(exact 2.5)` は `5/2` ですが `(exact 0.1)` は `1/10` ではなく `3602879701896397/36028797018963968` になります。無限大や NaN はエラーになります。

```scheme
(exact 2.5) ; => 5/2
(exact 2.0) ; => 2
(exact 0.1) ; => 3602879701896397/36028797018963968
```

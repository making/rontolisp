# expt

`(expt z1 z2)`

`z1` の `z2` 乗を返します。正確な底と正確な整数の指数なら結果は正確で、負の指数では分数になります。`(expt 0 0)` は `1` です。整数でない指数では、根が正確に求まる場合でも浮動小数点数になります（`(expt 4 1/2)` は `2.0`）。複素数の結果はありません。

```scheme
(expt 2 10) ; => 1024
(expt 2 -2) ; => 1/4
(expt 2.0 0.5) ; => 1.4142135623730951
```

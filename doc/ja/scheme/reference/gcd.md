# gcd

`(gcd n ...)`

引数の最大公約数を返します。結果は常に非負で、`(gcd)` は `0` です。`2.0` のような不正確な整数も受け付け、そのとき結果は不正確数になります。整数でない引数はエラーになります。

```scheme
(gcd 12 18) ; => 6
(gcd -12 18) ; => 6
(gcd) ; => 0
(gcd 2.0 4) ; => 2.0
```

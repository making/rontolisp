# (scheme read)

現在の入力ポート、または引数に渡したポートからデータを読み込みます。文字や行を読む手続きは `(scheme base)` のものです（[入力](library-base.md#input)）。

| 名前 | 例 | 結果 |
|---|---|---|
| `read` | `(read)` | 入力 `(1 "two" #\3)` に対して `(1 "two" #\3)` |

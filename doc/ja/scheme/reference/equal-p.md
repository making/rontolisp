# equal?

`(equal? obj1 obj2)`

構造を比較します。ペアとベクタは要素ごとに、文字列は文字ごとに比較し、それ以外は `eqv?` と同じ比較です。そのため `(equal? 2 2.0)` は `#f` です。`define-record-type` で作ったレコードはフィールドごとではなく同一性で比較します。

```scheme
(equal? '(1 (2 #(3))) '(1 (2 #(3)))) ; => #t
(equal? "abc" "abc") ; => #t
(equal? 2 2.0) ; => #f
```

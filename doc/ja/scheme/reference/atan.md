# atan

`(atan z)` `(atan y x)`

`z` の逆正接をラジアンで返します。引数が 2 つなら、両方の符号を考慮した点 (`x`, `y`) の角度を (-π, π] の範囲で返します。`(atan 0)` と、正の正確数 `x` に対する `(atan 0 x)` は正確数の `0` です。

```scheme
(atan 1) ; => 0.7853981633974483
(atan 1 0) ; => 1.5707963267948966
(atan 0 1) ; => 0
```

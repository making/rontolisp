# nth

`(nth n list)`

`list` の 0 始まりインデックス `n` の要素を返します。`n` がリストの長さ以上の場合、結果は `nil` になります。引数の順序に注意してください。インデックスが先で、その後にリストが来ます。

```lisp
(nth 2 '(a b c d)) ; => C
```

```lisp
(nth 10 '(a b c)) ; => NIL
```

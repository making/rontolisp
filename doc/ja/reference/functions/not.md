# not

`(not object)`

論理否定です。`object` が `nil`（偽）であれば `t` を、そうでなければ `nil` を返します。`nil` だけが偽の値であるため、nil 以外の任意の引数は `nil` を返します。挙動は `null` と同一です。真偽値を扱う文脈では `not` を、空リストの判定には `null` を選んでください。3つすべてのバックエンドで動作します。

```lisp
(not nil) ; => T
```

```lisp
(not 42) ; => NIL
```

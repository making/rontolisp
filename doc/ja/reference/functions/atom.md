# atom

`(atom object)`

`object` がコンスセルでなければ `t` を、そうでなければ `nil` を返します。コンスでないもの（数値、シンボル、文字列、文字、`nil`）はすべてアトムなので、`(atom nil)` は `t` になります。`consp` の正確な補集合です。3 つすべてのバックエンドで動作します。

```lisp
(atom 1) ; => T
```

```lisp
(atom '(1 2)) ; => NIL
```

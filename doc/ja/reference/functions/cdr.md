# cdr

`(cdr list)`

コンスセルの cdr、すなわちリストの最初の要素を除いた残りを返します。特別なケースとして `(cdr nil)` はエラーではなく `nil` になります。リストを扱う際は、より読みやすい同義語として `rest` を使えます。

```lisp
(cdr '(1 2 3)) ; => (2 3)
```

```lisp
(cdr nil) ; => NIL
```

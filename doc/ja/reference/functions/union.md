# union

`(union list1 list2)`

両方を集合として扱い、`list1` または `list2` のいずれかに現れるすべての要素を含むリストを返します。要素の比較は `eql` のみで行われ、`:test` や `:key` はありません。結果の要素の順序は未規定です。

```lisp
(union '(1 2 3) '(2 3 4)) ; => (4 1 2 3)
```

# car

`(car list)`

コンスセルの car（最初の要素）を返します。特別なケースとして `(car nil)` はエラーではなく `nil` になるため、リストの末尾まで辿っても安全です。リストを扱う際は、より読みやすい同義語として `first` を使えます。

```lisp
(car '(1 2 3)) ; => 1
```

```lisp
(car nil) ; => nil
```

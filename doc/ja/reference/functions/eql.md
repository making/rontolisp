# eql

`(eql x y)`

`eq` と同様ですが、加えて型と値が同じ 2 つの数値に対して `t` を返します。そのため、等しい浮動小数点数や等しい比は、`eq` でなくても `eql` で等しくなります。型が異なる数値は `eql` になりません（`(eql 3 3.0)` は `nil`）。コンスセルの中まで辿ることはしません。構造的な比較には `equal` を使ってください。3 つすべてのバックエンドで動作します。

```lisp
(eql 1.5 1.5) ; => t
```

```lisp
(eql 3 3.0) ; => nil
```

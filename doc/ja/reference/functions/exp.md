# exp

`(exp number)`

e の `number` 乗を浮動小数点数として返します。どのバックエンドも fdlibm で計算します（インタプリタと JVM では `StrictMath.exp`、WASM では同じアルゴリズムをランタイム関数として持ちます）。そのため桁はどこでも一致します: `(exp 1.0)` は `2.7182818284590455` です。

```lisp
(exp 0) ; => 1.0
```

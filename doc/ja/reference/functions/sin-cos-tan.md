# sin cos tan

`(sin radians)` `(cos radians)` `(tan radians)`

3 つの基本的な三角関数で、それぞれラジアン単位の角度を取り浮動小数点数を返します。`sin` は正弦、`cos` は余弦、`tan` は正接です。インタプリタと JVM バックエンドでのみ利用可能です (WASM では利用できません)。

```lisp
(cos 0) ; => 1.0
```

# phase

`(phase number)`

`number` の複素平面上の偏角を浮動小数点数で返す。複素数なら虚部と実部の `atan2`、非負の実数なら `0.0`、負の実数なら円周率である。4バックエンドすべてで動作し、どれも fdlibm の `atan2` なので桁はどこでも一致する。

```lisp
(phase 5) ; => 0.0
```

```lisp
(phase -5) ; => 3.141592653589793
```

# sqrt

`(sqrt number)`

`number` の平方根を浮動小数点数として返します。引数が完全平方数の場合でも同様です (`(sqrt 16)` は `4` ではなく `4.0` です)。結果は常に倍精度浮動小数点数です。3 つすべてのバックエンド (インタプリタ、JVM、WASM) で動作します。

```lisp
(sqrt 2) ; => 1.4142135623730951
```

# list

`(list &rest objects)`

評価された引数を順番に並べた、新たに割り当てられた真正リストを返します。引数がない場合は `nil` (空リスト) を返します。各引数が 1 要素になるため、ネストした呼び出しはネストしたリストを構築します。

```lisp
(list 1 2 3) ; => (1 2 3)
```

```lisp
(list) ; => nil
```

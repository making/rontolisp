# prin1

`(prin1 object &optional stream)`

`object` を読み戻し可能な形式で標準出力に書き出します。`print` とまったく同じですが、末尾の改行がありません。文字列は前後のクォート付きで、文字は `#\` 構文で出力されるため、出力は `read` で読み戻すことができます。オプションの stream 引数を指定すると、標準出力の代わりにそのストリームに出力されます。`object` を返します。

```lisp
(prin1 "hello")
```

```
"hello"
```

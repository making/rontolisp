# delete

`(delete item list)`

`remove` の破壊的版です。`item` と `eql` であるすべての要素を取り除いた `list` を返します。コピーするのではなくコンスセルをその場で変更します。リストの先頭が変わる可能性があるため、常に元の変数に頼らず戻り値を使ってください。

```lisp
(delete 2 '(1 2 3 2)) ; => (1 3)
```

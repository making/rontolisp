# equalp

`(equalp x y)`

[`equal`](equal.md) と同様ですが、文字列と文字は大文字小文字を区別せず、数値は数値としての値 (`=`) で比較します。コンスセルは再帰的に比較されます。簡易版: 配列・ハッシュテーブル・構造体は要素ごとの比較ではなく `eql` にフォールバックします。

```lisp
(equalp "ABC" "abc") ; => t
```

# signum

`(signum number)`

`number` の符号を示す `-1`、`0`、`1` のいずれかを、その数値型を保ったまま返します。整数または有理数の引数は整数の結果を、浮動小数点数の引数は浮動小数点数の結果 (例: `1.0`、`0.0`、`-1.0`) を返します。

```lisp
(signum -5) ; => -1
```

```lisp
(signum 3.5) ; => 1.0
```

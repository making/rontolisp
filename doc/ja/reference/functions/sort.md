# sort

`(sort list predicate)`

`predicate` を使って `list` を破壊的にソートします。`predicate` は 2 引数の比較関数で、第 1 引数が第 2 引数より前に来るべきときに非 nil を返します。リストのコンスセルはその場で並べ替えられるため、元の変数ではなく戻り値を使用してください。このソートは安定ではないため、`predicate` で等しいとみなされる要素の相対的な順序は未規定です。

```lisp
(sort (list 3 1 2) #'<) ; => (1 2 3)
```

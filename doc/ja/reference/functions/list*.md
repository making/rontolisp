# list*

`(list* object &rest more)`

先頭の引数を、末尾となる最後の引数の上にコンスします。最後の引数がリストの場合、結果は先頭に拡張された真正リストになります。リストでない場合、結果はドット対リストになります。引数を 1 つだけで呼び出すと、その引数をそのまま返します。

```lisp
(list* 1 2 '(3 4)) ; => (1 2 3 4)
```

```lisp
(list* 1 2 3) ; => (1 2 . 3)
```

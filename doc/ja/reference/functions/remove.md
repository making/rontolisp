# remove

`(remove item list)`

`item` に `eql` であるすべての要素を取り除いた `list` の要素からなる新しいリストを返します。元のリストは変更されません。破壊的なバージョンには `delete` を使用してください。比較は `eql` のみで行われます。

```lisp
(remove 2 '(1 2 3 2)) ; => (1 3)
```

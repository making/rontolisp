# equal

`(equal x y)`

構造的な等価性を判定します。コンスセルは car と cdr によって再帰的に比較され、文字列は 1 文字ずつ比較されます。それ以外のものについては `eql` と同様に動作します。`t` または `nil` を返します。3 つすべてのバックエンドで動作します。

```lisp
(equal '(1 2 (3)) '(1 2 (3))) ; => t
```

```lisp
(equal "abc" "abc") ; => t
```

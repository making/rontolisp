# cons

`(cons object1 object2)`

car が `object1`、cdr が `object2` である新しいコンスセルを確保して返します。cdr がリストの場合は結果はより長いリストになり、そうでない場合はドット対になります。これはリストを構築する基本的なプリミティブです。

```lisp
(cons 1 '(2 3)) ; => (1 2 3)
```

```lisp
(cons 1 2) ; => (1 . 2)
```

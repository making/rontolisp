# nreverse

`(nreverse sequence)`

各コンスセルの `cdr` を繋ぎ替えることで `sequence` を破壊的に反転し、新しい先頭を返します。元のセルが再利用され先頭が変わるため、戻り値を使用する必要があります。渡した変数はもはや反転後のリスト全体を指しません。元のリストを保持したい場合は代わりに `reverse` を使用してください。

```lisp
(nreverse (list 1 2 3)) ; => (3 2 1)
```

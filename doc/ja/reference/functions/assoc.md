# assoc

`(assoc key alist)`

連想リスト（`(key . value)` ペアのリスト）を検索し、car が `key` と `eql` である最初のペアを返します。一致するものがなければ `nil` を返します。返されるペアは連想リストと構造を共有します。キーではなく値で検索するには `rassoc` を使います。

```lisp
(assoc 'b '((a 1) (b 2))) ; => (b 2)
```

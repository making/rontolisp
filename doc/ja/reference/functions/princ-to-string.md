# princ-to-string

`(princ-to-string object)`

`princ` が `object` に対して出力するテキストを、文字列として返します。文字列にクオートを付けず、文字に `#\` プレフィックスを付けない人間が読みやすい形式です。何も出力されず、レンダリング結果を取得して返します。任意の値からテキストを組み立てるのに便利です。

```lisp
(princ-to-string '(1 "x")) ; => "(1 x)"
```

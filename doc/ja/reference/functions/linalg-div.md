# linalg:div

`(linalg:div a b)`

`a` を `b` で要素ごとに除算し、新しい packed double-float 配列を返します。どちらの被演算子もスカラーにでき、その場合はもう一方の被演算子の shape にブロードキャストされます。両方が配列の場合は shape が一致していなければなりません (一致しない場合はエラーを通知します)。

```lisp
(linalg:div #(1 2 3) 2) ; => #d(0.5 1.0 1.5)
```

# fresh-line

`(fresh-line)`

出力がまだ新しい行の先頭にない場合にのみ標準出力へ改行を書き込みます。カーソルがすでに桁 0 にある場合に空行を生むことはありません。nil を返します。二重改行のリスクなしに、後続の出力が独立した行から始まることを保証するために使います。

```lisp
(princ "a")
(fresh-line)
(princ "b")
```

```
a
b
```

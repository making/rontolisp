# expt

`(expt base power)`

`base` の `power` 乗を返します。整数の引数では結果は正確な整数になります (`(expt 2 10)` は `1024`)。どちらかの引数が浮動小数点数なら結果は浮動小数点数になります。3 つすべてのバックエンドで動作します。

```lisp
(expt 2 10) ; => 1024
```

```lisp
(expt 2.0 3) ; => 8.0
```

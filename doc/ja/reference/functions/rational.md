# rational

`(rational number)`

値が表す正確な有理数を返します。整数と分数はすでに正確なのでそのまま返され、浮動小数点数はその正確な2進値を答えます。複素数やその他の非実数値はエラーを通知します。正確な有理数を持たない NaN や無限大も同様です。

```lisp
(rational 1.5) ; => 3/2
```

```lisp
(rational 5) ; => 5
```

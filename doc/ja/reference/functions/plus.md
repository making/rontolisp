# +

`(+ &rest numbers)`

引数の合計を返します。引数がない場合は `0` を返します。すべての引数が整数のときは結果は整数、ratio が関与するときは ratio、いずれかの引数が浮動小数点数のときは浮動小数点数になります（汚染）。インタプリタと JVM では、整数の結果はオーバーフロー時に多倍長整数へ昇格します。WASM バックエンドは 31 ビット整数を使用し、昇格は行いません。

```lisp
(+ 1 2 3) ; => 6
```

```lisp
(+ 1.5 2.5) ; => 4.0
```

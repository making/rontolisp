# +

`(+ &rest numbers)`

引数の合計を返します。引数がない場合は `0` を返します。すべての引数が整数のときは結果は整数、ratio が関与するときは ratio、いずれかの引数が浮動小数点数のときは浮動小数点数になります（汚染）。整数の結果は、どのバックエンドでもオーバーフロー時に多倍長整数へ昇格します。

```lisp
(+ 1 2 3) ; => 6
```

```lisp
(+ 1.5 2.5) ; => 4.0
```

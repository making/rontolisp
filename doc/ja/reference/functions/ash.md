# ash

`(ash integer count)`

`integer` を `count` ビット分だけ算術シフトします。`count` が非負のときは左へ（より上位のビットへ向かって）、負のときは右へ（符号拡張付きで）シフトします。結果はどのバックエンドでも任意の大きさの正確な整数になります。

```lisp
(ash 1 4) ; => 16
```

```lisp
(ash 255 -4) ; => 15
```

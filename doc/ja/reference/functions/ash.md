# ash

`(ash integer count)`

`integer` を `count` ビット分だけ算術シフトします。`count` が非負のときは左へ（より上位のビットへ向かって）、負のときは右へ（符号拡張付きで）シフトします。インタプリタと JVM では結果は任意の大きさの正確な整数になりますが、WASM では値は 31 ビットの `i31` です。

```lisp
(ash 1 4) ; => 16
```

```lisp
(ash 255 -4) ; => 15
```

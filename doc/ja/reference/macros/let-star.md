# let*

`(let* ((var1 init1) (var2 init2)...) body...)`

`let` と同様ですが、束縛が逐次的に確立されます。各 `init` 形式は、同じ束縛リスト内でそれより前に束縛された変数を参照できます。すべての変数が束縛された後、ボディの各形式が順に評価され、最後の値が返されます。束縛ごとに1つずつ、ネストした `let` 形式に展開されます。

```lisp
(let* ((x 1) (y (+ x 1))) (list x y)) ; => (1 2)
```

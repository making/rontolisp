# prog2

`(prog2 first second body...)`

`first`、続いて `second`、その後に残りのボディ形式を順に評価し、`second` の値を返します。`prog1` と対をなしますが、*2番目*の形式の値を返す点が異なり、最初の形式が純粋にセットアップのための副作用である場合に便利です。

```lisp
(prog2 1 2 3) ; => 2
```

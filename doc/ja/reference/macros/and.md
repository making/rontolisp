# and

`(and expr1 expr2...)`

式を左から右へ評価し、いずれかが nil を返した時点で短絡してその nil を返します。すべての式が非 nil の場合は最後の式の値を返します。引数のない `(and)` は `t` を返します。ネストした `if` 形式に展開されるため、nil が見つかった後の式は評価されません。

```lisp
(and 1 2 3) ; => 3
```

# or

`(or expr1 expr2...)`

各式を左から右へ評価し、いずれかが非 nil の値を返した時点で短絡してその値を返します。すべての式が nil の場合は nil を返します。引数なしの `(or)` は nil を返します。ネストした `if` 形式に展開されるため、最初の真の式より後の式は評価されません。

```lisp
(or nil 2 3) ; => 2
```

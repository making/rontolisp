# psetq

`(psetq v1 e1 v2 e2 ...)`

並列代入: まずすべての右辺の式が評価され、その後にはじめて変数へ代入されます。そのため各値は変数の古い束縛に対して計算されます。これにより、一時変数を使わずに1ステップで変数の交換やローテーションを行えます。`psetq` は常に nil を返します。

```lisp
(let ((a 1) (b 2)) (psetq a b b a) (list a b)) ; => (2 1)
```

# the

`(the type form)`

`form` を評価し、その値をそのまま返します。型はチェックされないため（伝えるべきコンパイラ型システムが存在しません）、`the` は純粋なドキュメントです。型注釈付きのライブラリソースを変更なしにロードするための機能です。実行時の型検査には `check-type` を使ってください。

```lisp
(the integer (+ 1 2)) ; => 3
```

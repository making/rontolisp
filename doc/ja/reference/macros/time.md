# time

`(time form)`

`form` を評価し、標準出力に `; Elapsed real time: N ms` という形式の行を出力して、`form` が生成した値を返します。`N` はインタープリタと JVM バックエンドではミリ秒の整数、WASM ではミリ秒の浮動小数点数です。実時間（wall-clock time）を報告するため非決定的であり、実行ごとに変動します。以下の例ではタイミングの行を出力し、`3` を返します。

```lisp
(time (+ 1 2))
```

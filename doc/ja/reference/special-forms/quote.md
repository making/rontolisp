# quote

`(quote expr)` or `'expr`

`expr` を評価せずに返し、書かれたとおりのリテラルなデータ(シンボル、リスト、またはアトム)を生成します。これがリストリテラルやシンボルリテラルを作る方法です。リーダーマクロ `'expr` は `(quote expr)` の省略記法です。引数は決して評価されないため、`(quote (+ 1 2))` は `3` ではなく 3 要素のリストを返します。

```lisp
(quote (a b c)) ; => (a b c)
```

# if

`(if test then [else])`

`test` を評価し、それが nil でなければ `then` 分岐を評価して返し、そうでなければ任意の `else` 分岐を評価して返します。2 つの分岐のうち評価されるのは常に一方のみです。偽となる値は `nil` だけで、`0` や空文字列を含むその他すべての値は真とみなされます。`else` 分岐がなく `test` が偽の場合、結果は `nil` になります。

```lisp
(if (> 3 2) 'yes 'no) ; => yes
```

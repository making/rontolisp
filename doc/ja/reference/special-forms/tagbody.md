# tagbody

`(tagbody {tag | form}...)`

本体のフォームを順に評価します (値は捨てられます)。本体中の裸のシンボル (または整数) は *go タグ* で、[`go`](go.md) がそのタグの直後のフォームへ前方・後方を問わず制御を移すため、明示的なジャンプでループや状態機械を書けます。末尾に到達すると nil を返します。

現時点では**インタープリタのみ**でサポートされます。JVM / WASM コンパイラは未対応です。

```lisp
(let ((n 0))
  (tagbody
   top
    (incf n)
    (when (< n 5) (go top)))
  n) ; => 5
```
